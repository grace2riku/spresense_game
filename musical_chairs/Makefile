
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_SPRESENSE_GAME_MUSICAL_CHAIRS_PROGNAME)
PRIORITY = $(CONFIG_SPRESENSE_GAME_MUSICAL_CHAIRS_PRIORITY)
STACKSIZE = $(CONFIG_SPRESENSE_GAME_MUSICAL_CHAIRS_STACKSIZE)
MODULE = $(CONFIG_SPRESENSE_GAME_MUSICAL_CHAIRS)

ASRCS =
CSRCS =
MAINSRC = musical_chairs_main.c

include $(APPDIR)/Application.mk
