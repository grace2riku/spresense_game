
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_SPRESENSE_GAME_ELECTRIC_GUITAR_PROGNAME)
PRIORITY = $(CONFIG_SPRESENSE_GAME_ELECTRIC_GUITAR_PRIORITY)
STACKSIZE = $(CONFIG_SPRESENSE_GAME_ELECTRIC_GUITAR_STACKSIZE)
MODULE = $(CONFIG_SPRESENSE_GAME_ELECTRIC_GUITAR)

ASRCS =
CSRCS =
MAINSRC = electric_guitar_main.c

include $(APPDIR)/Application.mk
