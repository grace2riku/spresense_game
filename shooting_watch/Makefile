
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_SPRESENSE_GAME_SHOOTING_WATCH_PROGNAME)
PRIORITY = $(CONFIG_SPRESENSE_GAME_SHOOTING_WATCH_PRIORITY)
STACKSIZE = $(CONFIG_SPRESENSE_GAME_SHOOTING_WATCH_STACKSIZE)
MODULE = $(CONFIG_SPRESENSE_GAME_SHOOTING_WATCH)

ASRCS =
CSRCS = shooting_watch_gpio.c
MAINSRC = shooting_watch_main.c

include $(APPDIR)/Application.mk
